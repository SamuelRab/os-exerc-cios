let numero = prompt("Digite o número"); // Variável que armazena o número digitado pelo usuário.

if(isNaN(numero)) {
    alert("Número inválido!");
  } // Indica a invalidez de uma entrada de informação que não seja um número.

  if (numero % 5 == 0) {
    alert("Ele é múltiplo de 5!");
    
} // Condição que indica se o número digitado é múltiplo de 5.

else {
    alert ("Ele não é múltiplo de 5!");
    
    } // Condição que indica se o número digitado não é múltiplo de 5.
