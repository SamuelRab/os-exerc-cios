let idade = prompt("Digite sua idade") // Variável que armazena a idade digitada pelo usuário.

if(isNaN(idade)) {
    alert("Valor inválido!");
  } // Indica a invalidez de uma entrada de informação que não seja um número.


 if (idade > 18) {
        alert("Maior de idade!")
    } // Condição que avalia se o número digitado é maior que 18.

    else {
        alert ("Menor de idade")
        } // // Condição que avalia se o número digitado é menor que 18.
      
  


